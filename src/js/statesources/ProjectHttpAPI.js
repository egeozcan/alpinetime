export default Marty.createStateSource({
  getProject: function (id) {
    var reject, resolve;
    return (new Promise(function () {
      [reject, resolve] = arguments;
    })).then(function () {

    });
  }
});